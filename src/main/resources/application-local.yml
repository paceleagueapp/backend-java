server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: always

spring:
  datasource:
    url: jdbc:mysql://paceleague-db.cf2mmwae4jfh.ap-northeast-2.rds.amazonaws.com:3306/paceleague?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: admin
    password: PaceLeague!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
    open-in-view: false

  data:
    redis:
      host: 127.0.0.1
      port: 6379

app:
  jwt:
    issuer: paceleague
    access-token-ttl-seconds: 900        # 15분
    refresh-token-ttl-seconds: 1209600   # 14일
    secret: "YOUR_SUPER_LONG_SECRET_KEY_CHANGE_ME_!!!"

logging:
  level:
    org.springframework.security: DEBUG